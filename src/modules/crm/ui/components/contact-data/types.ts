/**
 * `card`: la sección «Datos del cliente» del Contacto 360 (etiqueta a la
 * izquierda, 180px; acciones Confirmar · Corregir · ⋯).
 * `rail`: el bloque compacto del rail de contexto del inbox (340px; etiqueta
 * ENCIMA del valor; acciones Confirmar · Corregir; más «En esta conversación»).
 */
export type ContactDataVariant = "card" | "rail";
