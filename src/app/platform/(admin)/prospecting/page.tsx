import { ProvidersView } from "@/modules/platform/ui/features/prospecting/ProvidersView";

/** /platform/prospecting — proveedores externos de la captación de leads. */
export default function PlatformProspectingPage() {
  return <ProvidersView />;
}
