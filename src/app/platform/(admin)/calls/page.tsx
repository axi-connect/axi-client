import { CallsView } from "@/modules/platform/ui/features/calls/CallsView";

/** /platform/calls — aprovisionamiento Twilio: cuenta madre y números. */
export default function PlatformCallsPage() {
  return <CallsView />;
}
