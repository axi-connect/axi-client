import { DebuggerView } from "@/modules/platform/ui/features/quality/debugger/DebuggerView";

/** /platform/quality/debugger — depurador forense de conversaciones. */
export default function PlatformQualityDebuggerPage() {
  return <DebuggerView />;
}
