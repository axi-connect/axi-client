import { RunWizard } from "@/modules/platform/ui/features/quality/runs/wizard/RunWizard";

/** /platform/quality/runs/new — wizard de nueva ejecución (QA o estrés). */
export default function PlatformQualityNewRunPage() {
  return <RunWizard />;
}
