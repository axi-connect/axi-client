import { RunsView } from "@/modules/platform/ui/features/quality/runs/RunsView";

/** /platform/quality/runs — ejecuciones de QA y estrés (default de Calidad). */
export default function PlatformQualityRunsPage() {
  return <RunsView />;
}
