import { DatasetsView } from "@/modules/platform/ui/features/quality/datasets/DatasetsView";

/** /platform/quality/datasets — golden sets por tenant y capacidad (upgrade F4). */
export default function PlatformQualityDatasetsPage() {
  return <DatasetsView />;
}
