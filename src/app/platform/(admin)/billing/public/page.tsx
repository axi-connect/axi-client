import { PublicPreviewView } from "@/modules/platform/ui/features/billing/PublicPreviewView";

/** /platform/billing/public — lo que devuelve GET /public/pricing. */
export default function PlatformBillingPublicPreviewViewPage() {
  return <PublicPreviewView />;
}
