import { MarginView } from "@/modules/platform/ui/features/billing/MarginView";

/** /platform/billing/margin — consola de margen: costo real, simulador y verja. */
export default function PlatformBillingMarginPage() {
  return <MarginView />;
}
