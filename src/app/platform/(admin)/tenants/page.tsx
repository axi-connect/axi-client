import { TenantsView } from "@/modules/platform/ui/features/tenants/TenantsView";

/** /platform/tenants — lista completa (el endpoint no pagina; tabla client-side). */
export default function PlatformTenantsPage() {
  return <TenantsView />;
}
