import { TenantWizard } from "@/modules/platform/ui/features/tenants/wizard/TenantWizard";

/** /platform/tenants/new — alta de tenant (3 pasos + revisión). */
export default function NewTenantPage() {
  return <TenantWizard />;
}
