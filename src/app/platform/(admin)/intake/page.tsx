import { IntakeView } from "@/modules/platform/ui/features/intake/IntakeView";

/** /platform/intake — guiones y entrevistas de puesta en marcha. */
export default function PlatformIntakePage() {
  return <IntakeView />;
}
