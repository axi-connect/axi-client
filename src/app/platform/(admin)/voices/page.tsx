import { VoicesView } from "@/modules/platform/ui/features/voices/VoicesView";

/** /platform/voices — curaduría del catálogo de voces IA (§10.5). */
export default function PlatformVoicesPage() {
  return <VoicesView />;
}
