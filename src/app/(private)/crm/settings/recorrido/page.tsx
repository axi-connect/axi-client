import { JourneyEditor } from "@/modules/crm/ui/components/settings/journey/JourneyEditor";

/** El recorrido del cliente: tipo y cadencia por etapa, plantillas por nicho (gate crm:manage). */
export default function CrmSettingsJourneyPage() {
  return <JourneyEditor />;
}
