import { ImportsManager } from "@/modules/crm/ui/components/settings/ImportsManager";

/** Wizard + historial de imports CSV (gate contacts:import). */
export default function CrmSettingsImportsPage() {
  return <ImportsManager />;
}
