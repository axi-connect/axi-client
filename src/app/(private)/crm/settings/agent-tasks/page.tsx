import { AgentTaskSettingsView } from "@/modules/crm/ui/components/settings/AgentTaskSettingsView";

/** Política del motor de tareas de agente (lectura crm:read, escritura crm:automate). */
export default function CrmSettingsAgentTasksPage() {
  return <AgentTaskSettingsView />;
}
