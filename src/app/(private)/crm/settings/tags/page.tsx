import { TagsManager } from "@/modules/crm/ui/components/settings/TagsManager";

/** CRUD de etiquetas (gate crm:manage). */
export default function CrmSettingsTagsPage() {
  return <TagsManager />;
}
