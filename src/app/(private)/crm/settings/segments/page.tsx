import { SegmentsManager } from "@/modules/crm/ui/components/settings/SegmentsManager";

/** Segmentos guardados + builder del DSL (gate crm:manage). */
export default function CrmSettingsSegmentsPage() {
  return <SegmentsManager />;
}
