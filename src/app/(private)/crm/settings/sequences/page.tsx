import { SequencesManager } from "@/modules/crm/ui/components/settings/SequencesManager";

export const metadata = { title: "Secuencias · CRM" };

export default function CrmSequencesPage() {
  return <SequencesManager />;
}
