import { AgentStudioView } from "@/modules/agents/ui/AgentStudioView";

/** Crear un agente: el estudio a ancho completo (D5). */
export default function NewAgentPage() {
  return <AgentStudioView mode="create" />;
}
