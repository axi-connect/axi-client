import { CommercialSkeleton } from "@/modules/commercial/ui/CommercialSkeleton";

/** Carga de ruta: skeleton estructural (cabecera + hero + listas), LOADING.md §1. */
export default function ComercialLoading() {
  return <CommercialSkeleton />;
}
