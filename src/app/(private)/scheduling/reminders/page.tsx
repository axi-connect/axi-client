import { RemindersView } from "@/modules/scheduling/ui/RemindersView";

/** Recordatorios de la agenda: automáticos de cita + manuales (one-shot y recurrentes). */
export default function SchedulingRemindersPage() {
  return <RemindersView />;
}
