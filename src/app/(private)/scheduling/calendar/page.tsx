import { CalendarView } from "@/modules/scheduling/ui/CalendarView";

/** Calendario de citas (vista default de /scheduling). */
export default function SchedulingCalendarPage() {
  return <CalendarView />;
}
