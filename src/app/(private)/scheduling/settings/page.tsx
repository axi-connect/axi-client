import { SchedulingSettingsView } from "@/modules/scheduling/ui/SchedulingSettingsView";

/** Configuración de la agenda: reglas de agendamiento + horario de atención. */
export default function SchedulingSettingsPage() {
  return <SchedulingSettingsView />;
}
