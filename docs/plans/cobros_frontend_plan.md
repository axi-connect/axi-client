# Plan frontend — Programa «Cobros, documentos y TRM»

> Parte B (cliente) del plan maestro `axi-server/docs/plans/cobros_program.md` (aprobado 2026-09-16). Misma disciplina que el resto del proyecto: **mockup HTML navegable aprobado por el dueño antes de escribir UI**, servidor primero por contrato (`npm run api:types` tras cada fase del backend), un PR por fase a `main` desde el worktree `.claude/worktrees/cobros`. Todo lo técnico en inglés, la documentación en español; wire en `snake_case`.

## 1. Hallazgos del cliente que condicionan el diseño (verificados en `3fce465`)

- **Medios de pago** ya es un slice propio (`src/modules/payments/`) montado en `/settings/company/pagos` como pestaña de Mi empresa (`CompanySettingsNav` la muestra solo con `hasCapability("sales")`). El DTO no tiene moneda ni tasa. El seed de navegación apunta a `/settings/sales`, aliasado en `core/config/routes.ts::NAV_PATH_ALIASES`.
- **Pedidos** (`src/modules/orders/`): `OrderDto.payments[]` ya es un array con `amount_cents` por pago, pero nada suma `paid`/`outstanding`; `ORDER_TRANSITIONS`/`KANBAN_COLUMNS` no tienen estado parcial; `ReportPaymentDialog` es el único punto donde se registra un pago (monto opcional, prellenado con el total); `PaymentProofViewer` es el visor de archivo existente; **no hay `orders/public.ts`** (crm e inbox lo van a necesitar); no existe UI de creación manual de pedido.
- **Ficha del contacto** (`crm/contacts/[contactId]/page.tsx`): `ScorePanel`, `CopilotPanel`, `TagsEditor`, `ContactDealsCard`, `ContactTimeline` y el nuevo `ContactDataPanel` (CRM F1). No hay card de pedidos ni área de documentos. `ContactDealsCard` es la forma a clonar.
- **Rail del inbox**: `context-rail/registry.tsx` es el punto de extensión declarado («añadir un panel es añadir una entrada aquí»); no hay panel de pedidos/pagos.
- **Plantillas**: `marketing/ui/components/MessageTemplateField.tsx` (textarea + chips de variables + vista previa en burbuja) espeja `marketing/domain/template.ts`. No hay editor rich-text ni visor PDF. El único helper de descarga por blob es `platform/infrastructure/api/quality-report.ts`.
- **Entitlements**: `shared/auth/entitlements.{store,hooks}.ts` (`useEntitlements(): {loaded, hasCapability}`, fail-open, single-flight). Seis call sites (`sales`, `calls`). No existe noción de feature por tenant.
- **Tipo de negocio**: `onboarding/domain/niches.ts` (9 nichos) solo en el paso 1 del onboarding; el microcopy promete «Lo puedes cambiar después en Ajustes de empresa» pero `company.config.tsx` solo tiene `industry` texto libre. `onboarding/public.ts` no exporta `NICHES`.
- **Dinero**: `core/lib/format.ts::formatMoney(cents, currency='COP')` (es-CO, 193 usos; inserta espacio duro → nunca asertar literales) y `PriceInput`. Sin selector de moneda para el tenant. La TRM solo existe en `platform/` (parámetro versionado «una versión se sucede, no se edita»).
- **Catálogo**: `catalog/ui/components/ProductAttributesSection.tsx` decide el input con una cadena `if/else` por tipo y cae a texto libre para cualquier tipo desconocido → un atributo `date` renderizaría texto libre sin error (hallazgo del auditor). Hay que volverlo exhaustivo.
- **Design system**: `StatusBadge` exige un `StatusMap` en `domain/` (estados = superficie `secondary` + punto, nunca tinte); `StatTile`, `Timeline`, `FieldList`, `NavTabs`, `SegmentedControl`, `DetailSheet`, `DataTable` + `usePaginatedList` disponibles. `@testing-library/user-event` **no** está instalado (usar `fireEvent`).

## 2. Arquitectura de información y navegación

**Sidebar** (lo decide el seed `rbac_ui_module` del backend con el nuevo `feature_code`; el cliente solo resuelve paths):

- Grupo **Ventas**: Pedidos `/orders` · **Cartera `/orders/receivables`** (feature `collections`, `collections:read`) · Catálogo · Envíos · **Pagos `/settings/payments`** (renombra «Métodos de pago»; `payment_methods:read`).
- **Mi empresa** `/settings/company`: General (+ «Tipo de negocio») · Sucursales · **Funciones** `/settings/company/funciones`.
- Documentos **no** tienen ítem propio: viven en el pedido, el contacto y el rail del inbox; su editor es una pestaña del hub Pagos.
- `core/config/routes.ts`: quitar el alias `/settings/sales`; `next.config.ts`: redirect 308 `/settings/company/pagos → /settings/payments` (URL compartida hoy).

> **Cambio de F5 (2026-09-18), decidido al hacer el mockup:** los recordatorios salen de «Plan de pagos» y ganan **pestaña propia**. El motivo no es que no cupieran: «Plan de pagos» es **el trato** (anticipo, cuotas, fechas) y se congela en cada pedido al confirmarlo, mientras la cadencia y los textos son **la operación** y se leen vivos en cada envío — la misma línea que el servidor tuvo que trazar en F5 tras el bloqueante. Mezclarlas en una pestaña le pediría al dueño que recordara cuál de los ajustes que tiene delante alcanza a los clientes que ya debe y cuál no; separadas, cada pestaña tiene una sola respuesta, y la de Recordatorios lo dice en su pie.

**Hub Pagos** `src/app/(private)/(content)/settings/payments/{layout,page,loading}.tsx` + sub-rutas reales `plan/`, `moneda/`, `documentos/` (patrón `CompanySettingsLayout` + `NavTabs`): **Medios** (mueve `PaymentMethodsTab`) · **Plan de pagos** (feature `payment_plans`) · **Recordatorios** (feature `collections`, F5) · **Moneda y TRM** (`fx_quotes`) · **Documentos** (`documents`; `SegmentedControl` Contrato · Recibo · Estado de cuenta · Cuenta de cobro + ajustes de emisión/envío). Las pestañas se filtran con `useFeatures` y esperan a `loaded` (nunca pintar-y-quitar).

**Cartera** en `/orders/receivables` dentro de `src/app/(private)/orders/`: hereda `OrdersLayout` y su slot `@sheet`, así que una fila abre el mismo rail `/orders/[orderId]` interceptado (segmento estático gana al dinámico).

## 3. Slices a crear / extender

### `shared/auth/features.{store,hooks}.ts` (F1)
Espejo 1:1 de `entitlements.{store,hooks}.ts`: `useFeaturesStore` (`load(userId)` → `GET /me/features`, single-flight, reset por usuario), `hasFeatureIn(features, status, code)`, `useFeatures(): {loaded, hasFeature(code), featureSource(code)}`. **Fail-open** como entitlements: la garantía «Savage no ve planes» es del dato (el sidebar ya llega filtrado por `/me/navigation`) y el backend responde `403 features/feature_disabled`. `API_ERROR_CODES.featureDisabled` en `core/api/problem.ts` + mensaje en `core/lib/error-messages.ts`. Invalidación local al guardar en `FeaturesTab` o al cambiar el nicho.

### `companies` (extender, F1)
`ui/forms/config/company.config.tsx`: campo `niche_code` (Select con `NICHES` desde `onboarding/public`, que exporta `NICHES`/`nicheByCode`), `toUpdateCompanyDTO` lo envía. Nueva pestaña `/settings/company/funciones` → `ui/components/features/FeaturesTab.tsx` + `FeatureSwitchRow.tsx` (nombre, descripción, `Switch`, chip de origen «Por defecto del nicho» / «Activado por ti» / «Fijado por la plataforma» → deshabilitado con tooltip y `settings_hint`), `infrastructure/services/features-service.adapter.ts` (`PUT /features/:code`). `CompanySettingsNav.tsx`: General · Sucursales · Funciones (sale «Medios de pago»).

### `platform` (extender, F1)
`TenantTabs.tsx` + «Funciones» (`/platform/tenants/[tenantId]/features`), `ui/features/tenants/detail/TenantFeaturesView.tsx` (tabla feature → Heredar / Forzar ON / Forzar OFF + motivo; muestra lo que ve el tenant), `infrastructure/api/hooks/use-tenant-features.ts` (TanStack Query, `GET/PUT /platform/tenants/:id/features(/:code)`).

### `payments` (extender, F2/F4/F5)
`domain/fx-settings.ts` (`FxSettingsDTO`: `settlement_currency`, `spread_bps`, `manual_rate?`, `show_indicative_quotes`; `FxRateDTO`), `domain/payment-policy.ts` (`PaymentPolicyDTO`: anticipo %, estrategia, nº cuotas, días antes del servicio, gracia, `reminder_days_before`, `overdue_reminder_days`, canales, plantillas), `infrastructure/services/payment-settings-service.adapter.ts` (`GET/PUT /fx/settings`, `GET /fx/rates/latest`, `GET/PUT /collections/settings`), `ui/components/PaymentsHubNav.tsx`, `ui/forms/FxSettingsForm.tsx` + `forms/config/fx-settings.config.tsx`, `ui/components/FxRateCard.tsx` («hoy a TRM 3.100,45 · Superfinanciera · vigente 16 sep», `StatTile`), `ui/forms/PaymentPolicyForm.tsx` + `forms/config/payment-policy.config.tsx` (cadencias como chips numéricos; plantillas con `TemplateTextField`). `public.ts` añade `PaymentsHubNav`, `useFxSettings`.

### `collections` (nuevo, F4/F5)
```
domain/payment-plan.ts        PaymentPlanDTO, InstallmentDTO, INSTALLMENT_STATUS_MAP (pending→neutral, due_soon→warning,
                              overdue→destructive, partially_paid→info, paid→success), planProgress(plan) → {paid_cents, balance_cents, pct, next_due}
domain/receivable.ts          ReceivableRow, ListReceivablesParams, RECEIVABLE_FILTERS (Todo · Al día · Por vencer · Vencido), agingBucket(days)
infrastructure/services/collections-service.adapter.ts   GET /collections/receivables(/stats), GET /collections/plans/:id | by-order/:order_id,
                              PUT …/schedule, POST …/promises, PATCH …/promises/:id, PATCH /collections/plans/:id, POST …/notes, POST …/reminders
infrastructure/stores/collections.store.ts               stats + realtimeVersion
infrastructure/realtime/use-collections-socket.ts        collections.plan_updated | installment_paid | reminder_updated
infrastructure/hooks/use-order-plan.ts
ui/ReceivablesView.tsx        tiles + filtros + tabla (usePaginatedList) + banner «N cambios nuevos»
ui/tables/receivables.config.tsx     Cliente · Pedido · Total · Pagado · Saldo · Próxima cuota · Vence · Mora · Estado · Último recordatorio · acciones
ui/components/{ReceivablesStatsTiles,ReceivablesFilters,PaymentPlanBlock,InstallmentList,PlanEditorDialog,
                RegisterInstallmentPaymentDialog,PaymentProgressChip,PromiseDialog,SendReminderDialog}.tsx
public.ts                     PaymentPlanBlock, PaymentProgressChip, RegisterInstallmentPaymentDialog, planProgress, INSTALLMENT_STATUS_MAP, useOrderPlan
```
`RegisterInstallmentPaymentDialog` es la evolución de `orders/ui/components/kanban/ReportPaymentDialog.tsx`: medio de pago, **monto obligatorio** (`PriceInput`, `parseMoneyToCents`), selector de cuota con «Sugerido: cuota 2 · $…», referencia. `ReceivablesStatsTiles`: Por cobrar · Vencido (`destructive`) · Próximos 7 días (`warning`) · Cobrado 30 d (`success`). Móvil: filas en cards.

### `orders` (extender, F2/F3/F4/F8)
Crear **`orders/public.ts`** (`listOrders`, `getOrder`, `OrderStatusBadge`, `orderNumberLabel`, `ORDER_STATUS_LABELS`, tipos) para `crm`/`inbox`/`collections`. `domain/order.ts`: `service_date`, `payment_state`, `paid_cents`, `balance_cents`, `fx {base_currency, base_total_cents, rate, source, at}` en `OrderRow`; `formatOrderTotal(order)`. `OrderDetailRail`: total dual «US$ 7.000 ≈ $ 21.700.000 (TRM 3.100 del 12/03)» en cabecera y totales; «Pagado / Saldo»; sección **Plan de pago** (`PaymentPlanBlock` de `collections/public` con `hasFeature('payment_plans')`; sin plan → CTA «Crear plan de pago» si `canManage`); «Registrar pago» → «Registrar abono» cuando hay plan; sección **Documentos** (`DocumentsList order_id` + `IssueDocumentMenu`, feature `documents`); «Fecha del servicio» en Detalles. `PaymentReviewDialog`: monto obligatorio prellenado con el saldo + aviso de sobrepago. **Kanban sin columna nueva**: `OrderCard` pinta `PaymentProgressChip` («$1,2 M / $4,9 M · 2 de 4») cuando `balance_cents > 0`. `orders.store.ts` escucha `order.payment_verified` y `collections.installment_paid` para `refreshOrder`. `OrderTimeline`: `payment_state_changed`, `currency_frozen`, `service_date_set`. `OrderNotificationTemplatesForm`: renderiza `payment_received` (y la `checkout_link` hoy huérfana).

### `catalog` (extender, F2)
`ProductAttributesSection.tsx`: mapeo **exhaustivo** por `attribute.type` (`text | number | boolean | select | date`) con `<input type="date">` para `date`; sin fallback implícito. Formulario de variante: campo «Fecha de servicio» cuando el tipo de producto declara el eje `date`; deshabilitado con motivo en variantes gobernadas por integración.

### `documents` (nuevo, F7/F8/F9)

> **F7 cliente implementado (2026-09-22).** Mockup aprobado por el dueño: https://claude.ai/artifact/Cu8hiMbdG84sNW3RNUsWDK (12 artboards; fuente `docs/design/mockups/document-template-editor.build.py`). **Ubicación: Mi empresa › Documentos (`/settings/company/documentos`)**, no Pagos — los documentos son el papel de la EMPRESA y los consume cualquier proceso; la pestaña `Documentos` del hub Pagos desaparece (`DOCUMENTS_TAB_READY` y `PAYMENTS_DOCUMENTS_PATH` borrados, no apagados). Slice `documents` autónomo con `public.ts` desde el día uno: `DocumentsTab`, `DocumentTemplateEditor`, `TemplatePreviewFrame`, `DocumentKindTabs`, `DocumentSettingsForm`, adapters y `useTemplatePreview`; en F8 se añaden `DocumentsList`/`IssueDocumentMenu`/`SendDocumentDialog` con un `subject {kind, id}` genérico (la forma de `DocumentSubjectRef` del servidor). Tipos, bloques permitidos y variables llegan por el wire desde `GET /document-types`. La hoja es `<iframe sandbox="" srcDoc referrerPolicy="no-referrer">` con las fuentes del host inyectadas (`public/fonts/poppins`, `public/fonts/nexa`; `next.config.ts` sirve `/fonts/:path*` con `Access-Control-Allow-Origin: *` porque el iframe es de origen opaco) y zoom 50/65/80/100 %; `useTemplatePreview` debounce 500 ms + `AbortController` + salto por huella + `blocked` sin llamada mientras haya variables desconocidas; bloqueado/sin conexión son una BARRA sobre la hoja, no un velo. `FeatureDisabledState` pasó a `shared/` con vuelta parametrizable. Estado «sin permiso» (`document_templates:manage`) distinto del «sin función». Ajustes de F7: emisor y numeración (`DynamicForm` con verja de sucio); emisión/envío automático llegan en F9. **Pendiente del dueño (auditoría UX):** continuar una numeración existente (campo «siguiente número» por tipo, editable con el contador virgen) y el alcance del móvil en F7 (hoy: lista, edición y hoja apilada; sin pantalla aparte).

> **F8 cliente implementado (2026-09-23).** Mockup premium aprobado por el dueño: https://claude.ai/artifact/5tFrTFhVnrGsRVVAri1WjG (11 artboards; fuente `docs/design/mockups/order-rail-documents.build.py`). La idea: **un documento emitido es un hecho con número, y se ve como papel** — cada fila lleva un papelito blanco (`PaperMark`, blanco también en oscuro) con la línea de su tipo, el nombre, y debajo número en mono · fecha · páginas; una acción visible («Ver», URL firmada FRESCA al clic y `window.open(…, "noopener")`) y el resto en «…» (Regenerar, Copiar número); «Emitir» es un popover que ofrece solo lo que el catálogo declara emitible sobre esa entidad (`issue_subject`), dice «ya emitido» con su número cuando la política es uno-por-entidad (`issue_policy`, del wire — el cliente no espeja la idempotencia) y anuncia lo que saldrá. Estados sin fondo de color: barra fina al generar, motivo legible + «Reintentar» al fallar (mismo número; tres RONDAS → regenerar), «desactualizado» en ámbar como dato (`order.updated_at > created_at`). **Decisión del dueño:** el contrato desactualizado se REGENERA con los mismos datos (uno por reserva); no hay «emitir de nuevo» para el contrato. `DocumentsList` es una y se gatea sola (función `documents` + permiso de lectura; sin `documents:manage` no hay Emitir ni «…» y el pie lo dice): la monta el rail del pedido tras Pagos y `ContactOrdersDocumentsCard` del 360 (pedidos con saldo primero + la misma lista por contacto, sin Emitir). `orders/public.ts` nace aquí. Vivo por WS: `document.issued|failed` en `core/realtime/events.ts` (payload con `type_label`), `useDocumentsSocket` recarga si el evento concierne a la entidad y en RECONEXIÓN (segunda subida, no un flag que se apaga al caer); `notification-target` manda la campanita al pedido o a la ficha. Mi empresa › Documentos gana «siguiente número» por tipo junto a su prefijo: bloqueado y explicado en cuanto salió el primero (`numbering.next[code].started`), y en el PUT solo viaja lo que cambió (`start_at`). El panel `orders` del rail del inbox queda fuera de F8 (declarado en el plan del servidor §6.1).

> **F9 cliente implementado (2026-09-24).** Mockup aprobado por el dueño: https://claude.ai/artifact/Wsdxsu55ZagiCimZ8iqGTw (11 artboards; fuente `docs/design/mockups/send-document-dialog.build.py`, que importa el rail de F8). La idea: **el diálogo dice lo MISMO que hará el motor**. `SendDocumentDialog` pide `GET /documents/:id/send-options` y LEE sus cuatro ramas de WhatsApp —ventana abierta (el PDF al chat), fuera con plantilla (sale el aviso «documento_listo» y el PDF cuando responda), fuera sin plantilla (deshabilitada con la razón; «Configurar plantilla» solo con `document_templates:manage`), sin canal— y si la ficha tiene correo; el cliente **no recalcula** la ventana de 24 h (`domain/delivery.ts`: `whatsappAvailability`, `emailAvailability`, `defaultChannel`, con test que falsifica el recálculo). La ficha es la única dirección de registro: sin correo no hay campo; con correo, «usar otro correo solo esta vez» viaja como `to_email` sin tocar la ficha. Sin preflight (red caída) las dos tarjetas quedan activas y el servidor revalida. El 202 devuelve `{delivery, document}` y la fila hace `upsert` sin releer; el 422 «sin correo» queda inline; el 409 «ya va en camino» avisa y relee. La **entrega es una tercera línea de la fila por canal** (`deliveryLine`: glifo del canal en cápsula, hecho + detalle + hora; tono en el texto, nunca fondo; punto que late en `queued`): «Enviado por WhatsApp», «Salió el aviso» (HSM), «No salió: razón» (ámbar), «No se pudo enviar: razón» (rojo) con la salida en la misma línea —Reintentar por el mismo canal, o «Enviar por correo» cuando la ventana está cerrada sin plantilla—; sin `documents:manage` las líneas existen sin botones y el pie lo dice; un reemplazado que se envió lo sigue diciendo. `delivered` es inalcanzable y se lee como enviado. «Enviar» va primero en «…» (solo `rendered`). WS: `document.delivery_updated` en `core/realtime/events.ts` (con `DocumentEventIdentity` común a los tres `document.*`), `useDocumentsSocket` recarga si concierne —la política de F8 intacta: el WS avisa, `refresh` relee—. Mi empresa › Documentos gana **«Emisión y envío automáticos»** en el MISMO `DynamicForm` (un solo Guardar): «Cuándo se emite el contrato» en tres tarjetas excluyentes (`contract_issue`; los dos booleanos viejos en true → «al confirmar», que dispara primero), «Recibo automático», cuatro interruptores de envío que solo gobiernan lo que sale solo (pista ámbar si WhatsApp está encendido sin plantilla, con `useWatch`), y la plantilla de respaldo (nombre con patrón de Meta; idioma vacío → `es`; idioma sin nombre → error en el nombre). Códigos `documents/contact_without_email|delivery_in_flight|no_counterparty` en `problem.ts` + `error-messages.ts`; la campanita `document.delivery_failed` va al pedido (misma familia). Cierra el hallazgo abierto de F3: el timeline dice **cuánto** se verificó y, si el operador corrigió, lo que el cliente había reportado. Deviaciones del mockup, ambas declaradas: el aviso «en camino» no ofrece «Ver chat» (el documento no conoce la conversación) y el diálogo usa el `Dialog` de la casa también en móvil (no hay hoja inferior en el sistema).

```
domain/document.ts     DocumentDTO, DocumentKind, DOCUMENT_KIND_LABELS, DOCUMENT_STATUS_MAP, DELIVERY_STATUS_MAP, DELIVERY_SKIP_LABELS, isOutdated(doc, order)
domain/template.ts     espejo puro de la whitelist: DOCUMENT_TEMPLATE_VARIABLES[kind], TEMPLATE_VARIABLE_LABELS, invalidDocumentTemplateVariables, límites
domain/settings.ts
infrastructure/services/{documents,document-templates,document-settings}-service.adapter.ts
infrastructure/hooks/use-documents.ts               useDocuments({contact_id?|order_id?}) con version de realtime
infrastructure/lib/open-document-file.ts             GET /documents/:id/file → abre la presignada (patrón PaymentProofViewer)
infrastructure/realtime/use-documents-socket.ts      document.issued | failed | delivery_updated
ui/components/{DocumentsList,DocumentRow,SendDocumentDialog,IssueDocumentMenu,DocumentDeliveryStatus}.tsx
ui/components/templates/{DocumentTemplateEditor,ClauseListEditor,TemplatePreviewFrame,DocumentTemplatesNav}.tsx
ui/forms/DocumentSettingsForm.tsx + forms/config/document-settings.config.tsx
public.ts              DocumentsList, IssueDocumentMenu, SendDocumentDialog, DOCUMENT_KIND_LABELS, useDocuments, tipos
```
`DocumentTemplateEditor`: dos columnas — bloques (título, intro, cláusulas ordenables con subir/bajar/quitar/añadir sin librería DnD, nota de totales, firmas, pie, color de acento) | `TemplatePreviewFrame` (`<iframe sandbox="" srcDoc>` con marco de hoja A4 y zoom; `POST /document-templates/:kind/preview` con debounce 400 ms). «Restablecer al modelo de Axi». Aviso de variables inválidas antes del 422. `SendDocumentDialog`: radio WhatsApp/Email; email deshabilitado con motivo si el contacto no tiene correo; aviso de ventana de 24 h; 202 → chip «enviando…» hasta `document.delivery_updated`. `IssueDocumentMenu`: Contrato · Estado de cuenta · Cuenta de cobro (el recibo es automático).

### `shared/components/features/template-text-field/TemplateTextField.tsx` (F5)
Genérico extraído de `marketing/ui/components/MessageTemplateField.tsx` (`{value, onChange, variables, labels, maxLength, error, preview?}`), que pasa a componerlo. Lo usan plantillas de recordatorio (`payments`) y bloques de documento (`documents`).

### `crm` (extender, F4/F8)
`ui/components/contact-detail/ContactOrdersDocumentsCard.tsx` («Pedidos y documentos»: últimos pedidos con saldo vía `orders/public` + `DocumentsList contact_id` vía `documents/public`), montado bajo `ContactDealsCard` en `crm/contacts/[contactId]/page.tsx`.

### `inbox` (extender, F4/F8)
`context-rail/registry.tsx`: `ContextPanelDef` gana `feature?: string`; entrada `{id: 'orders', label: 'Pedidos y cobros', icon: ShoppingCart, permission: 'orders:read', feature: 'payment_plans', Panel: OrdersPanel}`; `panels/OrdersPanel.tsx` (pedido activo: saldo, próxima cuota, dual money, `PaymentPlanBlock` compacto, `DocumentsList` con enviar; consume solo barrels). El rail filtra por `useFeatures`.

### Transversales
`core/realtime/events.ts` (`InboxServerEvents`): `order.payment_verified`, `order.currency_frozen`, `collections.plan_updated | installment_paid | installment_overdue | reminder_updated`, `document.issued | failed | delivery_updated`. `notifications/domain/notification-target.ts`: familias `"collections."` → `/orders/{order_id}` o `/orders/receivables`; `"document."` → `/orders/{order_id}` o `/crm/contacts/{contact_id}`. `core/lib/format.ts::formatMoneyApprox(cents, currency, approx?: {currency, rate})` → `"US$ 3.500 ≈ $ 10.851.575"` (puro; la tasa siempre viene del snapshot del pedido, nunca de una global). `docs/architecture.md` §3.3: registrar los barrels nuevos (`orders`, `documents`, `collections`) y las ampliaciones (`onboarding`, `companies`).

## 4. Mockups de Fase 0 (`docs/design/mockups/`, convención `<name>.build.py` + `.html` + `.lucide.json`)

Light/dark, estados (cargando, vacío, error, 403 de capacidad/feature), móvil. DESIGN.md: coral única acción; violeta **o** ámbar como acento secundario, no ambos; badges = superficie `secondary` + punto; glass solo en flotantes; Poppins; AA.

| Fase | Mockup | Contenido |
|---|---|---|
| F1 | `company-settings-features.html` | «Tipo de negocio» en General; pestaña Funciones con origen y bloqueo por plataforma |
| F1 | `platform-tenant-features.html` | Overrides por tenant |
| F2/F4/F5 | `payments-hub.html` | Hub Pagos: Medios · Plan de pagos (+ recordatorios) · Moneda y TRM · Documentos; estados |
| F2 | `catalog-service-date.html` | Atributo tipo `date` en el editor de producto/variante, badge de fecha en la tabla de variantes, «Fecha del servicio» en el rail del pedido y el 422 de fechas mixtas |
| F3 | `orders-partial-payments.html` ✅ | **Publicado 2026-09-17** (lienzo «Cobros F3 · el abono», 8 artboards): rail con total dual, pagado, saldo y barra de cobro; diálogo de verificación con monto obligatorio prellenado al saldo, sin monto y con sobrepago; kanban con chip de cobro y sin columna nueva; saldo cubierto; timeline con los hechos nuevos; plantilla del aviso de abono con `{{amount}}` y `{{balance}}` |
| F5 | `collections-reminders.html` ✅ | **Publicado 2026-09-18** (lienzo «Cobros F5 · Recordatorios», 11 artboards): editor de cadencia y textos con **el hilo de WhatsApp como vista previa**, la plantilla apagada que deja un hueco visible, el aviso de HSM faltante donde se decide, «último aviso» en la Cartera, envío manual con el texto que sale por la FECHA de la cuota, e historial con lo que no se mandó y por qué |
| F4/F8 | `order-rail-plan-documents.html` | Rail con Plan de pago y Documentos con enviar (WhatsApp/Email, ventana 24 h, enviando/fallido/omitido), «desactualizado» |
| F4 | `collections-receivables.html` ✅ | **Publicado 2026-09-17** (lienzo «Cobros F4 · La cartera», 12 artboards): cartera con la respuesta antes que la tabla y **dos ejes separados** —el color dice cuánta prisa corre el dinero, el icono dice qué pasó con el servicio—; filtro «ya viajaron y deben» (D14); plan de pagos como lista agrupada en el pedido; registrar abono con la cuota propuesta y el reparto FIFO a la vista; reprogramar con la suma cuadrando en vivo y su descuadre; promesa de pago con lo que pausa; entregado y debiendo; la política junto al calendario que produce; el panel en la conversación; y la pantalla que Savage NO tiene. Absorbe `receivables.html`, `inbox-rail-orders-panel.html` y la sección «Plan de pagos» de `payments-hub.html` |
| F3 | ~~`orders-kanban-progress-chip.html`~~ | Absorbido por `orders-partial-payments.html`: el chip no se entiende sin el saldo del rail, así que van en el mismo mockup |
| F4 | `receivables.html` | Cartera: tiles, filtro segmentado, tabla con mora y acciones, vacío, móvil en cards |
| F4 | `inbox-rail-orders-panel.html` | Panel «Pedidos y cobros» |
| F4/F8 | `contact-360-orders-documents.html` | Card «Pedidos y documentos» |
| F7 | `document-template-editor.html` | Editor por bloques + vista previa A4; cláusulas; variable inválida; ajustes de emisión/envío + bloque fiscal |

## 5. Tests por slice

- **Config (puros, sin render):** `fx-settings.config.test.ts`, `payment-policy.config.test.ts`, `document-settings.config.test.ts`, `company.config.test.ts` (gana `niche_code`).
- **Dominio:** `collections/domain/__tests__/payment-plan.test.ts` (`planProgress`, mapa de estados completo), `receivable.test.ts` (`agingBucket`), `documents/domain/__tests__/{document,template}.test.ts` (labels completos, `isOutdated`, whitelist por kind, inválidas), `core/lib/__tests__/format.test.ts` (`formatMoneyApprox`), `notification-target.test.ts` (familias nuevas), `orders/domain/__tests__/order-state.test.ts`.
- **Stores:** `features.store.test.ts` (single-flight, reset por usuario, fail-open — copia de los tests de entitlements), `collections.store.test.ts`, `orders.store.test.ts` (eventos nuevos).
- **Componentes:** `DocumentsList` (estados; Enviar solo con `documents:manage`), `SendDocumentDialog` (email deshabilitado sin correo), `PaymentPlanBlock`, `RegisterInstallmentPaymentDialog` (monto obligatorio), `PaymentReviewDialog` (monto), `FeaturesTab` (switch bloqueado), `CompanySettingsNav.test.tsx` (pestañas), `PaymentsHubNav` (pestañas por feature), `registry` (filtro por feature), `ReceivablesView` (tiles + tabla con `usePaginatedList`), `DocumentTemplateEditor` (preview debounced; iframe con `sandbox`), `ProductAttributesSection` (`date`).
- Ritual: `npx tsc --noEmit` · `npm run lint` · `npx jest` · `npm run build` + revisión light/dark contra DESIGN-SYSTEM §11 antes de cada PR; `api:types:check` verde.

## 6. Orden de fases (alineado con el backend)

| Fase cliente | Tras backend | Entrega |
|---|---|---|
| F1 | F1 | `useFeatures`, `FeaturesTab`, `niche_code`, overrides en platform, redirect y alias |
| F2 | F2 | Hub Pagos (mover Medios), Moneda y TRM, `formatMoneyApprox`, `ProductAttributesSection` con `date`, fecha del servicio en el rail |
| F3 | F3 | Monto obligatorio al verificar, dual money, `PaymentProgressChip`, `orders/public.ts`, `payment_received` |
| F4 | F4 | Slice `collections`: Plan de pagos (settings), `PaymentPlanBlock`, «Registrar abono», Cartera, panel del inbox, card del 360 |
| F5 | F5 | Recordatorios: cadencia y plantillas (`TemplateTextField`), estados en Cartera, envío manual |
| F7 | F7 | Slice `documents`: editor de plantillas con vista previa, ajustes |
| F8 | F8 | `DocumentsList` en rail/360/inbox, emisión manual, descarga |
| F9 | F9 | `SendDocumentDialog`, estados de entrega |

## 7. Decisiones tomadas (revisables)

- **`useFeatures` fail-open (coherente con entitlements), y por qué aguanta.** La garantía «Savage no ve planes» NO la sostiene el cliente: `navigation.query.ts` filtra el sidebar por `feature_code` antes de mandarlo, así que un tenant sin la función no tiene ni el ítem. El fail-open solo decide PESTAÑAS dentro de una página a la que ya puede entrar (`/settings/payments` es alcanzable porque «Medios» no depende de ninguna función). El peor caso de un fallo de carga es una pestaña de más que responde 403 y lo explica; el fail-closed, en cambio, escondería una función que el tenant paga cada vez que Redis tosa. Lo que lo hace tolerable es que `paymentsHubTabs(has, ready)` devuelve solo «Medios» mientras `!ready`: nunca se pinta y se quita.
- **La pestaña existe cuando existe su pantalla** (`PLAN_TAB_READY`, `DOCUMENTS_TAB_READY` en `PaymentsHubNav`, y `SELECTABLE_ATTRIBUTE_TYPES` en catalog). Misma disciplina en los dos sitios: ofrecer algo cuyo destino aún no funciona es el «paso 2 sin el 3» que bloqueó F2. Se quitan al entrar F4 y F7.
- **Una función bloqueada se explica, no desaparece.** `detail()` devuelve `source` y `blocked_by`, y de ahí sale el texto. Al llegar F4, la pestaña de Cobranza con `blocked_by: {kind:'feature', code:'payment_plans'}` debe decir de qué depende en vez de esfumarse: desaparecer sin motivo es lo que deja al dueño sin saber por qué no tiene cobros.
- Vista previa de documento **server-side** (el layout HTML/CSS de impresión es del servidor; espejar solo la whitelist evita divergencias). Sin «Descargar PDF de prueba» en v1.
- Cartera en `/orders/receivables`, no de primer nivel.
- Sin librería de drag-and-drop para las cláusulas (subir/bajar).
- Descarga de PDF por URL presignada en pestaña nueva, no fetch-blob.

## 8. Estado de ejecución

| Fase | Estado |
|---|---|
| Paso 0 (este documento) | 2026-09-16 |
| Etapa A · mockups | Publicados 2026-09-16 como lienzo de diseño (17 artboards, light/dark/móvil/estados): https://claude.ai/artifact/3k7tchLhmyG1t5d632HeEp — `company-settings-features`, `platform-tenant-features`, `payments-hub` (Medios + Moneda y TRM), `catalog-service-date`. Copia en `docs/design/mockups/` (kit compartido `_axi_mockup_kit.py`; cada `*.build.py` genera su `.html` + `.lucide.json` y exporta artboards con `AXI_MOCKUP_ARTBOARDS_DIR`). **Pendiente de aprobación del dueño antes de tocar UI.** |
| F1 UI | **Implementada** (2026-09-16, mockups aprobados): `shared/auth/features.{store,hooks}` (espejo de entitlements, fail-open, `blocked_by` incluido), pestaña «Funciones» de Mi empresa (`FeaturesTab` + `FeatureSwitchRow`), «Tipo de negocio» en General, `CompanySettingsNav` = General · Sucursales · Funciones, consola `TenantFeaturesView` (Heredar / Forzar ON / Forzar OFF con motivo). Tests: `features.store`, `FeaturesTab`, `CompanySettingsNav`, `company.config`. |
| F2 UI | **Implementada** (2026-09-16): hub `Ventas › Pagos` (`/settings/payments`, `PaymentsHubNav` filtrado por función, «Medios» movida, redirect 308 del path viejo), pestaña «Moneda y TRM» (`FxRateCard`, `FxSettingsForm`, `FxSettingsTab` con sus tres estados y el 403 de función), `formatMoneyApprox`, «Fecha del servicio» en el rail del pedido y **`AttributeValueInput`** — control exhaustivo por tipo (`never` en el default) que sustituye las tres cadenas if/else (`ProductAttributesSection`, `VariantForm`, `VariantRowsEditor`) y habilita el tipo `date`. Tests: `fx-settings` (dominio), `FxSettingsTab`, `PaymentsHubNav`, `AttributeValueInput`, `format-money-approx`. |
| F3 UI | **Implementada** (2026-09-17, mockup aprobado): `paymentProgress` y `daysUntilService` en el dominio; `OrderRow` gana saldo, estado de cobro y fecha de servicio; `PaymentMeter` (un tramo por pago verificado, dibujado en un solo elemento); `OrderBalanceBlock` como primera sección del rail (cifra grande = cuánto falta, cuenta atrás de la salida, procedencia del precio congelado); `PaymentReviewDialog` con monto obligatorio, atajo al saldo, sobrepago explícito y **el resultado a la vista antes de confirmar**; medidor en la tarjeta del tablero; `payment_state_changed` y `currency_frozen` en el timeline; plantilla «Abono recibido» con sus variables propias; evento `order.payment_verified` en vivo. Tests: `payment-state` (dominio) y `PaymentReviewDialog`. **Desviación consciente del mockup:** el atajo «Anticipo del 30 %» no entra — ese porcentaje es la política de plan de pagos, que llega en F4; hoy solo existe «Usar el saldo completo», que es dato real. |
| F3 UI · auditoría | **Cerrada** (2026-09-17, ronda 1): cuatro hallazgos del auditor, todos con test que cae sin el arreglo. (1) **El campo del monto se podía editar y el servidor lo descartaba** cuando el pago ya traía cifra: se cierra del lado del operador — su monto manda, y el reportado por el cliente queda en el timeline (`reported_amount_cents`). (2) `daysUntilService` medía en UTC y restaba un día desde las 19:00 de Bogotá; ahora mide el día LOCAL, y `jest.env.ts` fija `TZ=America/Bogota` para que el caso de las 20:30 signifique lo mismo aquí y en CI. (3) `formatShortDate` corría un día toda fecha SIN hora (`new Date("2026-09-20")` es medianoche UTC) — bug vivo desde la Etapa A, no de F3, que afectaba a la salida de la expedición, la vigencia de la TRM y los `effective_from` de la consola. (4) El diálogo tenía el saldo en las dependencias del efecto y **se reiniciaba mientras el operador escribía** si otro verificaba un pago del mismo pedido: ahora se ancla a `payment.id`. Menor: «Cobrado» ya no topa la suma contra el total, que en un pedido cobrado de más daba la cifra falsa. |
| F3 UI · certificada | `audit-agent` certificó F3 al completo el 2026-09-17 (cliente `ce9a092`, servidor `2857a1f2`), revirtiendo con sus manos dos arreglos para ver caer sus tests. **Cerrado en F9 (2026-09-24):** el timeline dice cuánto se verificó y, si el operador corrigió el monto, lo que el cliente había reportado (`reported_amount_cents`); test `OrderTimeline.test.tsx`. **Hallazgo del auditor sobre la primera versión (a487d72):** «US$ 500» salía como «$ 500» porque el reportado va en la moneda del REPORTE y el verify como primera barrera congela el pedido a COP; el payload del servidor gana `reported_currency` (capturada antes de sobrescribir el pago) y el cliente la usa, cayendo a la del pedido en eventos viejos. Tests de los dos signos en ambos lados. **Decisión del dueño (D15):** el rail y el diálogo se quedan SIN gatear por función, iguales para todos los tenants; lo gateado es lo que de verdad es una función (cuotas, cartera, TRM, exigir el importe). **No desplegada**: el dueño decidió esperar. |
| F4 UI | **Implementada** (2026-09-17, mockup aprobado): slice `collections` con su dominio (`groupReceivables` y `sectionOf` llevan la decisión de diseño —las secciones son excluyentes y ordenadas por urgencia—, `daysUntil` en día LOCAL), adaptador, `ReceivablesView` en `/orders/receivables` (hereda el shell y el rail del tablero; el segmento estático gana al `[orderId]`), `ReceivableSectionList` con una sola diana por fila, `PaymentPlanBlock` en el rail del pedido (silencioso si no hay plan: 404 y 403 son el mismo silencio), `PaymentPolicyTab` con filas de ajustes y el calendario que producen al lado, «Cartera» en el menú con `feature_code: 'collections'` y la pestaña «Plan de pagos» del hub encendida. Tests: `receivable` y `payment-plan` (dominio), `ReceivablesView` (incluye el 403 sin la función), `PaymentsHubNav` actualizado. **Pendiente de la fase**: los diálogos de reprogramar y de promesa existen en el servidor y en el adaptador, pero su UI no — están en el mockup y entran con el siguiente tramo. |
| F4 UI · auditoría | **Cerrada** (2026-09-17, ronda 1): sin bloqueante propio, siete hallazgos. Los dos visibles: **la lista se callaba a las 100 filas** —pedía el total en `meta` y lo tiraba, así que a partir del deudor 101 la cartera mentía en la pantalla cuyo propósito entero es «a quién le escribo»— y **un fallo de red se disfrazaba de «no te debe nadie»**, que es el mismo principio del 403 sin aplicar una rama más allá. Además: la pestaña de política y su endpoint se gateaban por funciones distintas (`payment_plans` en el menú, `collections` en el servidor), así que «planes sí, cartera no» ofrecía una pestaña que respondía 403 y mandaba al dueño a encender algo ya encendido — la política se movió a su propio controlador bajo `payment_plans`, porque configura cómo NACEN los planes, no cómo se persiguen; «Cuotas además del anticipo» no hacía nada salvo con la estrategia `custom_count`, así que ahora la estrategia se elige y el número solo se ofrece cuando manda; el avión significaba «ya viajó» en la cartera y «cuota vencida» en el rail (dos significados a dos pantallas de distancia); el titular describía la cartera entera con la lista filtrada; y el `catch` mudo del plan se estrechó a 404 y 403 — un 500 sigue dejando la sección en blanco, pero ya deja rastro. |
| F4 UI · spike de ruta | **Hecho 2026-09-18, y encontró un fallo real.** Se montó una réplica exacta de la estructura de rutas (`layout` con slot `@sheet`, `@sheet/(.)[orderId]`, `[orderId]`, segmento estático hermano) dentro de la app, con la aplicación levantada y Chromium conducido por CDP. Resultado: **navegar del tablero a `/orders/receivables` interceptaba**. El slot pintaba «rail de un pedido llamado receivables» y el contenido se quedaba en el TABLERO — la Cartera no llegaba a pintarse nunca. La precedencia de estático sobre dinámico vale para el slot `children`, no para un slot paralelo. Se probó además meter un segmento estático dentro del propio `@sheet` (con `default.tsx` y con `page.tsx`): en carga dura sí gana, pero **en navegación soft la interceptación gana igual**, así que no sirve. Salida aplicada: la Cartera vive en `/receivables`, ruta propia. Abrir una fila navega al pedido, que renderiza el tablero con su rail ya abierto — a donde el operador quería ir. Lo verificado también: desde la Cartera aparte, abrir un pedido NO deja rail roto; la carga dura de un pedido sigue sin interceptar; y tablero → pedido sigue interceptando como siempre. |
| F8 UI | **Implementada** (2026-09-23, mockup premium aprobado; decisión del dueño: el contrato desactualizado se regenera con los mismos datos). Cliente `4b178dd`+docs; servidor certificado hasta `2b7aabe3`. Slice `documents`: `DocumentsList` (una, autogateada por función y permiso), `DocumentRow`, `IssueDocumentMenu` (opciones desde `issue_subject`/`issue_policy` del wire, mapa exhaustivo), `PaperMark`, `useDocuments` + `useDocumentsSocket` (reconexión = segunda subida), `openDocumentFile` (URL fresca + noopener); `orders/public.ts`; `ContactOrdersDocumentsCard` en el 360; WS `document.*`; campanita → pedido/contacto; «siguiente número» en Mi empresa › Documentos. Verjas propias: lint 0, tsc 0 (queda un error PREEXISTENTE en `inbox/…/ConversationPanel.test.tsx`, fuera de F8 y fuera del build), jest 375/3010, `next build` OK, `schema.d.ts` sin deriva. **Pendiente de la batería de `audit-agent`.** |
| F9 UI | **Implementada** (2026-09-24, mockup aprobado en su tercera versión: el dueño rechazó la vuelta «premium» de cristal y coral y pidió el lenguaje de F4–F8). Servidor certificado en `631e41a1`; `schema.d.ts` regenerado desde su OpenAPI, sin deriva. Slice `documents`: `domain/delivery.ts` (mapas exhaustivos, `deliveryLine(s)`, `whatsappAvailability` que LEE las cuatro ramas de `send-options`), `SendDocumentDialog`, tercera línea en `DocumentRow`, «Enviar» primero en «…», `useDocumentsSocket` con `document.delivery_updated`, ajustes «Emisión y envío automáticos», `OrderTimeline` con el monto verificado. Verjas propias: tsc 0 (sigue el error PREEXISTENTE de `ConversationPanel.test.tsx`), lint 0, prettier del slice limpio, jest y `next build` en la entrega. **Pendiente de la batería de `audit-agent`.** |
| F9 UI · certificada | `audit-agent` certificó F9 el 2026-09-24 (cliente `4e3b92c`, servidor `03de8893`). Su sonda —recalcular la ventana de 24 h en el cliente— tumbó 7 tests en los dos sentidos. Un hallazgo medio de dinero cerrado en la raíz: el timeline pintaba el monto reportado con la moneda del pedido; `payment_verified` gana `reported_currency`. |
| QA real F1–F3 | Hallazgos de la QA en navegador (`/root/axi/qa/cobros/`). **F2 medios** (`ffebd7b`): tasa manual vencida sin decirlo → aviso bajo la fecha; ejemplo «11.068.610» escrito a mano → `FX_SAMPLE_CENTS` + `sampleQuoteCents(latest)` compartidos con la tarjeta. **F3 dinero A–D** (`0336da3`): `ReportPaymentDialog` con `PriceInput` + `parseMoneyToCents` («1.000.000» ya no es NaN; lo ilegible frena con mensaje; `PriceInput.onInvalidChange`), precarga el saldo y no el total; verificar sin cifra no precarga y lo dice; «Rechazar» ya no queda mudo con reporte > saldo (`blocked` solo al verificar). **Bloqueante de producción ajeno a Cobros** (`f67e65d`): «Con variantes» tumbaba la app en crear producto (dos ramas de un ternario sin `key`); dónde aterriza lo decide el dueño. **Bajos pendientes:** miga con slugs/UUID, 4.ª pestaña de Mi empresa fuera de vista en móvil, chip de origen en Funciones sin tipo, placeholder «Roja · M» con eje fecha, 1 peso entre «Total − Cobrado» y «Falta», fecha de servicio en crudo, sin equivalente COP antes de confirmar, rail del pedido en móvil. |
| Avisos → sileo (§9.4) | **Implementado** (2026-09-24) según `cobros_avisos_sileo_plan.md`, un commit por slice (`documents` → `orders` → `collections` → `payments`). Títulos ≤ 34 con el dato en el cuerpo; el mensaje del servidor nunca en el título (7 llamadas); fuera los 13 `autoCloseMs`; cuatro avisos en línea a mano → `<Alert variant>` (el `Notice` del diálogo «Enviar», la mora sin plantilla de `RemindersTab`, el error de carga de plantillas de pedido con «Reintentar», y el fallo de «Ver más» de la cartera como estado al pie); el restablecer de plantillas → `showModal`; pastillas (§10) con el tinte en fondo e icono y el texto en `foreground`. `core/notifications/testing.ts` (`expectAlertContract`) hace que cada test de módulo compruebe el `showAlert` de su flujo con el peor caso real y pruebe que el título largo de antes NO pasa. `notify.promise` no se convirtió: R5 exige medir > 1 s en QA y la medición no ha llegado. Verificación visual pendiente de la QA real. |
| F5–F9 | Pendientes (cada una arranca con su mockup). **F4 arranca con D14**: un pedido se puede entregar con saldo pendiente, así que la Cartera tiene que distinguir al que YA VIAJÓ y debe —su caso más urgente— del que aún no ha salido. |
