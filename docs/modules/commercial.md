# Módulo Comercial (`src/modules/commercial/`) — F3 del método comercial

> Plan maestro: `axi-server/docs/plans/commercial_method_plan.md`. Mockup aprobado: `docs/design/mockups/commercial-route.html`. Marca: «axi vende progreso».

La ruta del mes: el tenant declara cuánto quiere vender, el servidor la traduce en resultados clave (ventas, cotizaciones, citas, contactados, conversaciones, llamadas) con su ritmo por día hábil, y la pantalla dice cuánto camino va recorrido, dónde debería ir hoy y a dónde llega si sigue así. Cada cifra lleva su procedencia («según tu historia» · «lo dijiste tú» · «supuesto para {nicho}»).

## Qué hay (F3)

- **`domain/`** (TypeScript puro, testeado): `commercial.ts` (contratos wire, `snake_case`), `labels.ts` (`KR_LABELS`, `SOURCE_LABELS`, `PACE_BADGES` como `StatusMap` de punto, `isOffPace`), `pace.ts` (`progressPct`, `expectedPct`, `gap` nunca negativo, `dailyRateNeeded`, y `displayStatus`/`isLearning`: el ESTADO del ritmo lo decide el servidor en `pace.status`; el cliente solo decide qué se pinta —cumplida gana, sin datos se aprende—), `weeks.ts` (semanas hábiles con los `weekdays` del tenant y el `today` que manda el servidor, sobre `core/lib/business-time`: `weekTicks`, `groupSeriesByWeek`, `weekOf`, `weekProgress` para una serie ACUMULADA), `format.ts` (`formatMillions` «$ 11,1 M», `formatRate`, `monthLabel`, `shortDay`; los enteros con `formatInteger` de `core/lib/commercial-units`), `copy.ts` (la voz «progreso»: `paceHeadline`, `missingLine`, `projectionLine`, `rateLine`, `learningLine`, `midMonthLine`, `seedLine`, `goalLead`; sin porcentajes negativos, ventas por día redondeadas hacia arriba, «1 venta al día»).
- **`infrastructure/`**: `services/commercial-service.adapter.ts` (`GET/PUT /commercial/goal`, `GET /commercial/plan`, `GET /commercial/plan/preview?target_cents=` con `AbortSignal`, `GET /commercial/pace?granularity=day|week`) y `stores/commercial.store.ts` (Zustand, patrón `Section<T>`: `goal`/`plan`/`pace`/`preview`, datos conservados en el refetch, `403 entitlements/capability_not_granted` → `blocker: "no_plan"`, número de secuencia por sección para descartar respuestas viejas entre cargas solapadas, vista previa con aborto que descarta respuestas tardías).
- **`ui/`**: `CommercialView` (sin permiso · bloqueado · cargando · error · sin meta · aprendiendo · con meta), `GoalEditorView` (`/comercial/meta`: `PriceInput` grande, atajos «Como el mes pasado / +10 % / +25 % / Otra cifra», lista viva «Lo que implica», `<details>` «Ajustar supuestos», aviso de mitad de mes), `CommercialSkeleton`, y los componentes `RouteLine` (el instrumento: SVG propio con coordenadas en %, gradiente con `id` por instancia, etiquetas HTML fuera del SVG, marcador hueco solo con esperado, prolongación punteada solo si la proyección supera lo recorrido, entrada con `spring.soft`), `RouteHero`, `PaceLine` (sobre `InlineFigures`, filete coral), `KeyResultList/Row` (badge de punto solo fuera de ritmo, regla fina de 96 px, mix de productos bajo «Ventas», «Contestadas X de Y» bajo «Llamadas hechas»; `detailHref` opcional: en F3 las filas NO enlazan porque las páginas de detalle no existen), `ActionList/Row` («Axi propone», único violeta; sin `proposals` dice «Las acciones que Axi propone llegan pronto.»), `SourceMark`, `GoalEmptyState`, `LearningNotice`, `CommercialBlockedState`, `GoalProgressBlock` (la franja del Panel).
- **`public.ts`**: solo `GoalProgressBlock` (lo único con consumidor hoy: `dashboard`).
- **Rutas**: `app/(private)/(content)/comercial/{layout,page,loading}.tsx`, `comercial/meta/page.tsx`. Navegación: icono `route` en `core/lib/icons.ts`, alias `/commercial → /comercial` en `core/config/routes.ts`. Gate: capacidad `crm` (`useEntitlements`, sin pintar el bloqueado mientras carga) + permisos `commercial:read` / `commercial:manage`.
- **Promociones** hechas por este módulo: `shared/features/charts/AreaTrend` (desde `dashboard`), `shared/features/inline-figures/InlineFigures` (molde de `AgentDigestLine`, que queda como envoltorio en `crm`), `shared/features/count-up/CountUpValue` (desde `analytics`; gana `progress` externo: el hero mueve cifra y línea con UN solo motor, `ui/hooks/use-entrance.ts`), `core/lib/business-time` (desde `scheduling`, con re-export fino) y el barrel `dashboard/public.ts` (`DashboardCard`, `MetricTile`) para `analytics`.

## Contrato TEMPORAL

`domain/commercial.ts` declara los tipos wire a mano bajo el comentario `// TEMPORAL (F3)`: el servidor de F3 se construyó en paralelo y `openapi.json` aún no traía `/commercial/*`. Al regenerar `core/api/schema.d.ts` hay que sustituirlos por `Schemas["..."]` y ajustar lo que difiera (nombres de campos, forma del preview). Lo que el cliente asume: `GET /commercial/goal` responde `{ goal | null, seed }` (nunca 404 por no tener meta); `GET /commercial/pace` trae `today` (YYYY-MM-DD en la TZ del tenant), `weekdays` (0–6 hábiles), `key_results[]` (con `answered_actual/expected` en `calls`), `series[]` diaria ACUMULADA, días hábiles y `data_sufficiency`; `GET /commercial/plan` trae `inputs` (tasas con procedencia, ventana y muestra), `figures`, `product_mix` y `status ready|incomplete`.

## Qué falta

- **F6**: `GET /commercial/proposals` y Aprobar/Rechazar (hoy `ActionList` se monta sin `proposals` y dice «Las acciones que Axi propone llegan pronto.»); las páginas de detalle `resultados/[key]` y `acciones/[id]` con `@sheet` + `DetailSheet` (hasta entonces `KeyResultList`, `PaceLine` y `ActionRow` NO enlazan: `detailHref`/`href` llegan con las páginas); `PaceTrend` sobre el `AreaTrend` promovido.
- **F7**: chip de meta en el `BriefingHero` de Axel (cuando `public.ts` publique los badges del ritmo), grupo `goal` en `SetupNextSteps` de Alba, `StageRatesCard`/`PipelineFlowCard` en Analítica.
- **F8**: eventos WS `commercial.*` (`stale: true` hoy solo se pinta lo que hay), verja completa y auditor.
- **Deuda** (con archivo:línea; auditoría F3 2026-09-23):
  - «Ajustar supuestos» expone solo lo que el modelo de la meta guarda (ticket y cotización → venta); el mockup enseña seis supuestos. Si el servidor persiste más tasas declaradas, el editor las gana campo a campo (`src/modules/commercial/ui/GoalEditorView.tsx`, bloque `<details>`).
  - El bloque del Panel no pinta skeleton mientras carga (misma regla que el banner de onboarding): aparece cuando hay dato (`src/modules/commercial/ui/components/GoalProgressBlock.tsx`, el `return null` bajo el `useEffect`).
  - El mes del título en el estado «sin meta» sale del reloj del navegador (`CommercialView.tsx` `monthKeyFallback`, ídem en `GoalEditorView.tsx`): sin meta no hay `pace.today`. Solo nombra el mes; ninguna cifra sale de ahí. Se salda cuando `GET /commercial/goal` traiga `today`.
  - `RouteLine` sin `progress` del padre (la franja del Panel) monta `SelfDrivenRouteLine` con su propio `useEntrance` (`src/modules/commercial/ui/components/RouteLine.tsx`); con `progress` no arranca ningún motor propio (un solo motor en el hero).
  - `Section<T>` es la sexta copia del patrón (`src/modules/commercial/infrastructure/stores/commercial.store.ts:20-33`): ver `docs/deuda_tecnica.md`.
  - Al montar `/comercial` con datos ya cargados (por el Panel) no se refresca: F8 trae los eventos WS que lo hacen innecesario (`CommercialView.tsx`, efecto de carga solo en `idle`).
